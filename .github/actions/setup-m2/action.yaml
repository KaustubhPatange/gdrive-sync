name: "setup-m2"
description: "setup-m2"
inputs: {}
outputs: {}
runs:
  using: "composite"
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Run npm install
      shell: bash
      run: |
        npm install
